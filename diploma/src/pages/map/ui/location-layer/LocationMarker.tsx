import { MarkerIcon } from "@shared/assets/icons/markers";
import { createMarkerIcon } from "@shared/libs/map";

export const LocationMarker = createMarkerIcon({
  icon: MarkerIcon,
  color: "#F97316",
});
